# CIPHER Login / Sign-up Logo Final Pass

## Scope
- Frontend only.
- Uses the approved `frontend/assets/cipher-spider-logo.png` team mark.
- Authentication center lockup is explicitly styled for both LOG IN and SIGN UP modes.
- Existing top authentication brand also continues to use the same mark.
- Logo is static: no hover, click, or animation transform.

## Verification
- `frontend/index.html` contains the team logo in the centered auth lockup and authentication header brand.
- `frontend/style.css` contains final explicit `.cipher-auth .auth-core` and `.cipher-auth .auth-core img` rules so the login and signup modes share the same presentation.
- Backend source files were not edited in this pass.
