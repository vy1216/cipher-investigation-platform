# CIPHER CSV Data-Source Fix QA

- Fresh local CSV graph preview now takes precedence over stale/default backend graph data while pending.
- Fresh local CSV timeline preview now takes precedence over older verified timeline events for the active case.
- Backend remains the authoritative source once no pending local preview is active.
- No backend files changed.
